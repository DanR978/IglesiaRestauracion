/**
 * IRD Page Transition — DISABLED.
 *
 * The full-screen logo overlay that played on every internal navigation has been
 * removed: links now navigate normally (instant), with no splash/loading screen.
 *
 * This module is intentionally a no-op so the <script> tags that reference it on
 * every page keep resolving without doing anything. To bring the effect back,
 * restore the previous implementation from git history.
 */
export {};
