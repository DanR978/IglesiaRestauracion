{
  "name": "iglesia-restauracion-divina",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Iglesia Restauración Divina — irdlex.org",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:css": "bash scripts/build.sh",
    "build:heads": "node scripts/build-heads.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "inline": "node scripts/inline-includes.mjs",
    "build:site": "bash scripts/build.sh && node scripts/build-heads.mjs && node scripts/build-sitemap.mjs && node scripts/inline-includes.mjs",
    "test": "node tests/special-events-lifecycle.test.mjs"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "terser": "^5.47.1",
    "vite": "^6.4.1"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
