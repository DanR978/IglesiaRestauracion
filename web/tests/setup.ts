// Vitest setup (S13): jest-dom's DOM matchers for every suite. The per-test
// DOM cleanup for component tests is registered by svelteTesting() in
// vitest.config.ts, not here.
import '@testing-library/jest-dom/vitest';
