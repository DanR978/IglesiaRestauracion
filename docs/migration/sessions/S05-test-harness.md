# Session S05 — Test harness + governance bootstrap

**Stream:** testing/cutover
**Depends on:** S02 (stacked on S04)   **PR:** <#___>   **Branch:** `migrate/S05-test-harness`
**Status:** 🟦 PR open

## Goal (one sentence)
Give `web/` its permanent quality gate — Vitest (jsdom + coverage), Playwright, the golden-master helper, ESLint+Prettier — and a CI workflow that enforces `check`+`lint`+`test`+`build` plus the "migration PRs must touch MIGRATION.md" ledger rule.

## In scope
- `web/vitest.config.ts` (environment **jsdom** per G-001; coverage-v8; `$lib` alias for pure-logic tests).
- `web/src/lib/test/golden.ts` — the golden-master helper (`itGolden(name, vectors, fn)`) consumed by S07/S08; `web/tests/fixtures/` with a self-test fixture; `web/tests/golden/harness.test.ts` (harness self-test) + `web/tests/unit/jsdom.test.ts` (G-001 guard: DOMParser must exist).
- `web/playwright.config.ts` + `web/tests/e2e/smoke.spec.ts` (preview-server smoke; **not** part of the CI gate — local `npm run test:e2e` after `npx playwright install chromium`).
- `web/eslint.config.js` (flat: @eslint/js + typescript-eslint + eslint-plugin-svelte + eslint-config-prettier), `web/.prettierrc` + `.prettierignore` (prettier-plugin-svelte).
- `web/package.json` scripts: `test`, `test:golden`, `test:e2e`, `lint` (= `prettier --check . && eslint .`), `format`; devDeps for all of the above.
- **`.github/workflows/ci.yml`** — job `web` (npm ci → check → lint → test → `BASE_PATH=/app` build, with the D-007 PUBLIC env from the same secrets deploy.yml uses) + job `ledger` (PRs labeled `migration` must touch `MIGRATION.md`; triggers include `labeled`/`unlabeled`).

## Out of scope (do NOT touch)
- No legacy tests/lint — legacy has no harness by design (D-012 explains how legacy fixes are tested).
- `deploy.yml` (S03/S04 own it). No app features, no Supabase client.
- DF-001's ported test — owed by **S51**, this session only builds the harness it lands in.

## Prereqs (read before writing code)
- [ ] Read `MIGRATION.md` (§0, §2 — esp. D-012 — §3 esp. G-001, G-018, G-019) and `CLAUDE.md`.
- [ ] Confirm S02 ✅ (stacked on S04's branch).
- [ ] Legacy source of truth: none (new tooling).
- [ ] Fixture / golden: `tests/fixtures/harness-sum.json` created here as the harness self-test; real fixtures arrive with S07/S08 (generated by running the LEGACY modules — the helper's doc header explains).

## Steps
1. Install devDeps; write the configs, helper, and sample tests; add the scripts.
2. `npm run format` once to normalize the scaffold to the new Prettier style (included in this PR).
3. Run the whole gate locally from a clean `.svelte-kit` state.
4. Push; confirm both CI jobs run green on this PR (it is labeled `migration` and touches `MIGRATION.md`, so `ledger` exercises its positive path).
5. **Human one-time follow-up after merge:** GitHub → Settings → Branches → protect `main` requiring checks `web` + `ledger`.

## Acceptance criteria
- [ ] `npm run test` green under jsdom (5 tests: golden self-test ×4 + DOMParser guard). ✅ verified locally
- [ ] `npm run test:golden` runs only `tests/golden/`. ✅
- [ ] `npm run lint` green (prettier + eslint, svelte-aware). ✅
- [ ] `npm run check` clean; `BASE_PATH=/app npm run build` succeeds. ✅ (214 files, 0 errors)
- [ ] `ci.yml` `web` job green on this PR; `ledger` job green (label present + MIGRATION.md touched).
- [ ] `typescript` stays `^5` (G-018 — do not let a dep bump pull TS 7).

## Verification gate
- [ ] Ran `docs/migration/VERIFICATION.md` — applicable boxes (#1 tests, #4 lint/format, #6 build, #8 scope, #12 types, #13 ledger); #5/#10/#11 N/A.

## How to resume if interrupted
- Branch: `migrate/S05-test-harness` (stacked on `migrate/S04-config-env`). Last known-good commit: see PR.
- Done so far: everything in scope written and verified locally; awaiting CI on the PR.
- Next concrete action: merge after S04; then the branch-protection human task.
- Landmines: vitest needs `.svelte-kit/` to exist (`check` runs `svelte-kit sync` first — keep the CI order check→test); `svelte-kit sync` generates the `$env/static/public` TYPE module from the env it sees, so `npm run check` needs the `PUBLIC_*` names declared (locally via `web/.env`, in CI via the job-level `env:` — a step-level env on build alone breaks check); e2e webServer build needs `web/.env`; G-019 applies to every local `BASE_PATH` build.

## On completion
- [ ] Update `MIGRATION.md`: status board, Current state, Next up.
- [ ] Open/refresh the PR; paste the gate results; link this file.
- [ ] Set this file's Status and PR number.
