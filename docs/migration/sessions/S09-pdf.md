# Session S09 — `pdf.ts` port (verbatim + types)


**Stream:** foundation
**Depends on:** S05 (test harness — `itGolden` + vitest/jsdom)   **PR:** <#___>   **Branch:** `migrate/S09-pdf-port`
**Status:** 🟦 PR open

## Goal (one sentence)
Port `js/lib/pdf.js` → `web/src/lib/pdf.ts` nearly verbatim (D-009), typed strict, keeping the CDN `<script>` + `window.pdfMake.vfs` loader pattern (G-007), with the pure builders golden- and structure-tested.

## In scope
- `web/src/lib/pdf.ts` — the ported module (all 13 legacy exports: `CHURCH`, `CHURCH_ADDRESS`, `CONTENT_W`, `loadPdfMake`, `loadPdfJs`, `imageDataUrl`, `churchLogo`, `sectionHeading`, `kpiBox`, `th`, `churchDocDef`, `savePdf`, `openPdf` — the ROADMAP row omits `churchLogo`/`CHURCH`/`CHURCH_ADDRESS`; the file is the truth).
- `web/tests/golden/pdf.test.ts` + fixtures `web/tests/fixtures/pdf-{constants,section-heading,th,kpi-box,church-docdef}.json` (15 vectors, generated by running the LEGACY module under node — D-012).
- `web/tests/unit/pdf.test.ts` — structural tests for the pure builders + import-safety.

## Out of scope (do NOT touch)
- The legacy `js/lib/pdf.js` and every legacy consumer (`report-builder.js`, `project-treasury.js`, `special-events-tab.js`, `waiver.js`) — live site untouched.
- `waiver.ts` (S10), treasury reports (S55/S56), registrations PDFs (S58), designer (S59) — they consume this module later.
- Converting pdfmake/pdf.js to npm imports — forbidden by G-007.
- DB/RLS, routes, CSS.

## Prereqs (read before writing code)
- [x] Read `MIGRATION.md` (§0, §2 D-009, §3 G-007) and `CLAUDE.md`.
- [x] S05 harness available (`web/src/lib/test/golden.ts`, vitest + jsdom config).
- [x] Legacy source of truth: `js/lib/pdf.js` (172 lines, branch `chore/cleanup`).
- [x] Fixtures: created THIS session by running the legacy module under node (root `package.json` is `"type":"module"`, so it imports directly; the module touches `window`/`document` only inside functions).

## Steps
1. Read `js/lib/pdf.js` end to end; inventory exports and browser touchpoints.
2. Generate golden fixtures from the LEGACY module (`node tools/capture-pdf-builders.mjs`): function-valued fields (kpiBox `layout` callbacks, churchDocDef `header`/`footer`/`background`) captured through an evaluation probe (footer at `(2,5)`, background at page 1 on 612×792pt).
3. Port to `web/src/lib/pdf.ts`: verbatim bodies + TS types; minimal local docDef interfaces (no npm pdfmake typings); typed `window` casts for `pdfMake`/`pdfjsLib`; `inBrowser()` guard so node/jsdom can import the module — loaders reject with the legacy Spanish messages when called outside a browser.
4. Write `tests/golden/pdf.test.ts` (same probe as the capture script) and `tests/unit/pdf.test.ts` (shapes asserted against legacy line-cited structures).
5. `tsc --noEmit` strict → 0 errors; `vitest run` → 31/31 pass; plain-node import smoke (`churchLogo()` → `null`, loaders reject in Spanish).

## Acceptance criteria
- [x] `web/src/lib/pdf.ts` typechecks under `strict` with 0 errors.
- [x] Golden parity: all 15 legacy-generated vectors reproduced by the port (`tests/golden/pdf.test.ts`).
- [x] Structural unit tests green: `CONTENT_W === 515`, builder shapes, `churchDocDef` header/footer/watermark wiring with a stub `wm`.
- [x] Module imports under plain node and jsdom without touching `window`/`document`; no CDN `<script>` injected at import time.
- [x] CDN loader pattern preserved exactly (G-007): pinned pdfmake `0.2.10` + `vfs_fonts`, pdf.js `3.11.174`, `window.pdfMake.vfs` memo check first.
- [ ] **Deferred to manual (browser step, parent records):** a scratch call downloads a `churchDocDef` PDF and `loadPdfJs` rasterizes a page (ROADMAP S09 acceptance). Not automatable headless; VERIFICATION.md forbids asserting pdfmake bytes anyway.
- [ ] Live legacy URLs unaffected (`git diff --stat` shows only `web/**`).

## Verification gate
- [ ] Ran `docs/migration/VERIFICATION.md` — boxes #1–#5 (install/check/lint/test/golden), #6 (build), #8 (legacy untouched), #12 (no secrets — module holds only public CDN URLs), #13 (ledger). Paste results in the PR.
- Session-local results so far (scratch harness): `tsc --noEmit` = 0 errors; `vitest run` = 2 files, 31 tests, all pass; node import smoke OK.

## How to resume if interrupted
- Branch: `migrate/S09-pdf-port`. Last known-good: see PR.
- Done so far: fixtures generated from legacy; module ported; golden + unit suites green in the scratch harness.
- Next concrete action: copy `web/src/lib/pdf.ts`, `web/tests/golden/pdf.test.ts`, `web/tests/unit/pdf.test.ts`, `web/tests/fixtures/pdf-*.json` from the S09 scratch tree into the real `web/` on the branch, then run the gate there.
- Landmines: the golden probe in `tests/golden/pdf.test.ts` MUST stay byte-identical to `tools/capture-pdf-builders.mjs` (function fields are evaluated, not serialized). `LOGO = '/resources/report-logo.png'` is root-absolute — fine during `/app` staging (same Pages artifact serves the legacy asset), but final cutover needs the asset under `web/static/resources/` (flag for S64). Do NOT convert the loaders to npm imports (G-007).

## On completion
- [ ] Update `MIGRATION.md`: status board (S09 ✅), Current state, Next up = S10 (`waiver.ts`, depends on S09); add a G-note if the `LOGO` cutover asset decision lands.
- [ ] Open/refresh the PR; paste the gate results; link this file.
- [ ] Set this file's Status and PR number.
